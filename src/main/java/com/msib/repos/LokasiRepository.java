package com.msib.repos;

import org.springframework.data.jpa.repository.JpaRepository;
import com.msib.models.Lokasi;

public interface LokasiRepository extends JpaRepository<Lokasi, Integer> {
    
}
